services:
  - type: web
    name: bigdeal
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:all
    startCommand: npm start
    healthCheckPath: /healthz
    envVars:
      - key: NODE_VERSION
        value: 22.15.0
